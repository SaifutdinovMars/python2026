tasks = ["забронировать зал", "заказать призы", "подготовить правила"]
tasks.extend(["разослать письма", "подготовить музыку"])
tasks.insert((tasks[0])"проверить оборудование")
tasks.pop("заказать призы" )
tasks.pop[-1] insert[1]
print(tasks)
