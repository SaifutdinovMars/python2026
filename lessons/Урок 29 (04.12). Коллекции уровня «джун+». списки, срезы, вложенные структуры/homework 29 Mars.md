# Домашнее задание 29. Коллекции уровня «джун+». списки, срезы, вложенные структуры

## Студент: Mars

### Итоги урока
- **Проблемные зоны:** синтаксис методов списков (`extend`) и корректные имена переменных; построение срезов по требованиям задачи.
- **Устойчивые умения:** подбор тематических списков, понимание необходимости считать длину и выводить элементы.
- **Приоритет повторения:** работа с методами списков без ошибок в синтаксисе, уверенное чтение/запись срезов с шагом.

### Домашние задания
Выполни задачи ниже, соблюдая требования к форматированию вывода. Если что-то не работает, добавь комментарий, что именно пошло не так.

#### 1. Списки и методы (исправляем ошибки блока 1)
Ты ведёшь каталог настольных игр для клуба. 
1. Создай список `base_games` минимум из трёх игр.
2. Скопируй его в `games = base_games.copy()`.
3. Расширь `games` списком `new_arrivals = ["Наместник", "Крылья", "Эверделл"]` через **один** вызов `games.extend(new_arrivals)`.
4. Выведи: общее количество игр, первую игру, центральную (по индексу `len(games)//2`).
5. Напечатай нумерованный список через `enumerate(..., start=1)`.
6. В конце выведи список игр, название которых длиннее 8 символов (через цикл или list comprehension).

> Проверь, что нигде нет точек после `extend`, а переменная `games` определена до использования.

#### 2. Срезы расписаний (исправляем ошибки блока 2)
Есть расписание электричек:
```python
schedule = ["06:05", "06:32", "06:59", "07:26", "07:53", "08:20", "08:47", "09:14", "09:41", "10:08"]
```
Выполни и подпиши каждый вывод:
1. Первые три отправления `schedule[:3]`.
2. Последние четыре `schedule[-4:]`.
3. Отрезок со 2 по 6 индексы **включительно** (используй `schedule[2:7]`).
4. Каждый второй рейс, начиная с нулевого индекса (`schedule[0::2]`).
5. Список без первого и последнего элементов (`schedule[1:-1]`).
Убедись, что переменная называется одинаково во всей программе и что в срезах нет синтаксических ошибок.

#### 3. Мутация списков задач
Составь план подготовки к клубному турниру:
1. `tasks = ["забронировать зал", "заказать призы", "подготовить правила"]`.
2. Добавь в конец `tasks.extend(["разослать письма", "подготовить музыку"])`.
3. Вставь после первого элемента напоминание "проверить оборудование".
4. Удали пункт "заказать призы" по значению, если он ещё в списке.
5. Перемести последний элемент на вторую позицию с помощью `pop` и `insert`.
6. Выведи итоговый список.

#### 4. Вложенные структуры
Создай список словарей `events`, описывающих игровые встречи:
```python
events = [
    {"day": "Пт", "time": "19:00", "theme": "евро-игры", "host": "Катя"},
    {"day": "Сб", "time": "14:00", "theme": "кооперативы", "host": "Антон"},
    {"day": "Вс", "time": "12:00", "theme": "варгеймы", "host": "Катя"},
]
```
Сделай выводы:
1. `День: тема (ведёт host)` для каждой встречи.
2. Список дней, когда встречу ведёт переменная `target_host = "Катя"`.
3. Новый список только с вечерними встречами (`time >= "18:00"`).
4. Количество уникальных тем (используй `set`).

Успехов! Сфокусируйся на чистом синтаксисе и точных срезах.
